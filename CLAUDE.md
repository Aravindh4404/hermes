# CLAUDE.md — Project Briefing
*Auto-generated by context-manager on 2026-05-25 16:00*
*Do not edit manually — regenerate with `python run.py --repo .`*

---

## Project Overview

- **Branch**: static_h
- **Last commit**: Set microtask queue true by default

Summary:
Change MicrotaskQueue default from false to true in Ru

## README Summary

# Hermes JS Engine
[![MIT license](https://img.shields.io/badge/license-MIT-blue.svg)](https://github.com/facebook/hermes/blob/HEAD/LICENSE)
[![npm version](https://img.shields.io/npm/v/hermes-engine.svg?style=flat)](https://www.npmjs.com/package/hermes-engine)
[![PRs Welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg)](https://github.com/facebook/hermes/blob/HEAD/CONTRIBUTING.md)
<img src="./doc/img/logo.svg" alt="Hermes logo - large H with wings" align="right" width="20%"/>

Hermes is a JavaScript engine optimized for fast start-up of [React Native](https://reactnative.dev/) apps. It features ahead-of-time static optimization and compact bytecode.

If you're only interested in using pre-built Hermes in a new or existing React Native app, you do not need to follow this guide or have direct access to the Hermes source. Instead, just follow [these instructions to enable Hermes](https://reactnative.dev/docs/hermes).

> Noted that each Hermes release is aimed at a specific RN version. The rule of thumb is to always follow [Hermes releases](https://github.com/facebook/hermes/releases) strictly. Version mismatch can result in instant crash of your apps in the worst case scenario.

If you want to know how to build and hack on Hermes directly, and/or integrate Hermes built from source into a React Native app then read on.

**[Hermes Blog](doc/blog/README.md)** - Technical articles and deep dives into Hermes internals.

The instructions here very briefly cover steps to

## Languages

- JavaScript: 5968 files
- C++: 835 files
- TypeScript: 112 files
- Rust: 111 files
- Python: 100 files
- C: 89 files
- Java: 35 files
- Swift: 4 files
- Kotlin: 3 files
- TypeScript/React: 1 files
- MATLAB/Objective-C: 1 files

**Total source files**: 13175

## Configuration Files

- `.github/workflows`

## Folder Structure

Key top-level directories: `API/` (JSI/ABI/NAPI/sandbox), `lib/` (compiler + VM source),
`include/hermes/` (headers), `tools/` (hermesc/shermes/hbc tools), `test/` (lit tests),
`unittests/` (gtest), `benchmarks/`, `external/` (llvh/asmjit/dtoa/pglite deps),
`unsupported/` (Rust crates/juno).

Use `gbrain search "<query>"` or `Glob` for file lookup — full file listing removed for size.

## Working with This Codebase

- Run `python query.py "<question>"` to semantically search the codebase
- Run `python run.py --repo .` to refresh this file after major changes
- See `storage/dependency_graph.json` for import/dependency relationships

---
*Context Manager — Layers: CLAUDE.md | AST Graph | RAG Embeddings*




## GBrain Configuration (configured by /setup-gbrain)
- Mode: local-stdio
- Engine: pglite
- Config file: ~/.gbrain/config.json (mode 0600)
- Setup date: 2026-05-28
- MCP registered: yes (user scope)
- Artifacts sync: off
- Current repo policy: read-write
- Source: gstack-code-hermes (135 markdown docs indexed)

## GBrain Search Guidance (configured by /sync-gbrain)
<!-- gstack-gbrain-search-guidance:start -->

GBrain is set up and synced on this machine. The agent should prefer gbrain
over Grep when the question is semantic or when you don't know the exact
identifier yet.

**This worktree is pinned to a worktree-scoped code source** via the
`.gbrain-source` file in the repo root (kubectl-style context). Any
`gbrain code-def`, `code-refs`, `code-callers`, `code-callees`, or `query`
call from anywhere under this worktree routes to that source by default —
no `--source` flag needed. Conductor sibling worktrees of the same repo
each have their own pin and their own indexed pages, so semantic results
match the actual code on disk in this worktree.

Two indexed corpora available via the `gbrain` CLI:
- This worktree's code (auto-pinned via `.gbrain-source`).
- `~/.gstack/` curated memory (registered as `gstack-brain-<user>` source via
  the existing federation pipeline).

Prefer gbrain when:
- "Where is X handled?" / semantic intent, no exact string yet:
    `gbrain search "<terms>"` or `gbrain query "<question>"`
- "Where is symbol Y defined?" / symbol-based code questions:
    `gbrain code-def <symbol>` or `gbrain code-refs <symbol>`
- "What calls Y?" / "What does Y depend on?":
    `gbrain code-callers <symbol>` / `gbrain code-callees <symbol>`
- "What did we decide last time?" / past plans, retros, learnings:
    `gbrain search "<terms>" --source gstack-brain-<user>`

Grep is still right for known exact strings, regex, multiline patterns, and
file globs. Run `/sync-gbrain` after meaningful code changes; for ongoing
auto-sync across all worktrees, run `gbrain autopilot --install` once per
machine — gbrain's daemon handles incremental refresh on a schedule.

<!-- gstack-gbrain-search-guidance:end -->

## gstack
Available skills: /office-hours, /plan-ceo-review, /plan-eng-review, /review, /ship,
/qa, /document-generate, /document-release, /learn, /setup-gbrain, /sync-gbrain,
/investigate, /cso, /autoplan, /retro, /careful, /freeze, /guard, /gstack-upgrade.